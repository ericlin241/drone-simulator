# 無人機模擬器操作指南

## 1. 開始使用

1. 在電腦開啟 GitHub Pages 網址的 `index.html`。
2. 等待左上角出現 QR Code 與「等待手機遙控器連線」。
3. 使用手機相機掃描 QR Code。
4. 手機頁面顯示「已連線」後，點選「解鎖馬達」或「自動起飛」。
5. 推動搖桿控制無人機；放開後搖桿會自動回中。

電腦與手機不必位於同一個 Wi-Fi，但都必須能連上網際網路。請保持電腦端頁面開啟；重新整理電腦頁面會產生新的 Peer ID，因此手機也要重新掃描 QR Code。

## 2. 美國手與日本手

手機畫面頂端可隨時切換控制模式。選擇會保存在手機瀏覽器中，下次開啟時沿用。

### 美國手 Mode 2

- 左搖桿上下：Throttle，控制上升／下降
- 左搖桿左右：Yaw，控制機頭左旋／右旋
- 右搖桿上下：Pitch，控制前進／後退
- 右搖桿左右：Roll，控制向左／向右移動

### 日本手 Mode 1

- 左搖桿上下：Pitch，控制前進／後退
- 左搖桿左右：Yaw，控制機頭左旋／右旋
- 右搖桿上下：Throttle，控制上升／下降
- 右搖桿左右：Roll，控制向左／向右移動

切換模式時，所有控制值會先自動歸零，避免無人機因軸向交換而突然移動。

## 3. 飛行功能

- **解鎖馬達／鎖定馬達**：允許或停止手動控制。鎖定時無人機不再接受搖桿推力。
- **內八解鎖**：兩支實體搖桿同時推向內下角並保持 0.8 秒。按鈕會顯示進度，完成後手機短震提示。
- **自動起飛**：自動解鎖並上升至約 3 公尺高度，之後進入懸停狀態。
- **自動降落**：緩慢下降到停機坪，落地後自動鎖定馬達。
- **重設位置**：將無人機移回停機坪上方，並清除速度與自動飛行狀態。
- **緊急停止**：立即歸零控制、停止動力並鎖定馬達。這是模擬器功能；真實無人機的緊急停止行為可能不同。

手機上方會接收電腦端回傳的高度、速度、航向與模擬電量。電腦右上角則顯示飛行狀態、高度和電量。

### 搖桿靈敏度

中央滑桿可在 35%～100% 間調整，預設為 65%，設定會保存在手機瀏覽器。控制器另外具有 11% 中心死區與 Expo 曲線：中心附近動作較細緻，推到外圈時才逐漸增加輸出。當某一軸已有明確輸入、另一軸只有少量偏差時，軸向輔助會濾除小幅斜推，降低上升時意外偏航或平移的機會。

## 4. 考場、碰撞與飛行姿態

場地依交通部民用航空局 AC 107-005D「基本級未達 2 公斤無人直昇機與多旋翼機測場示意圖」的主要尺寸建模：外框 15 m × 15 m、左右標點距中心線各 6 m、前後標點距離 5 m、H 起降圓直徑 2 m，並標示操作人與監評區。[民航局術科測驗資料頁](https://www.caa.gov.tw/article.aspx?a=2621&lang=1)

淡藍色 8 字線是提供初學者辨識路徑的輔助線，並非取代監評指示。正式考場內保持淨空；樹木、機棚、訓練門及護欄配置在 15 m 外框之外。

無人機前進時會機頭下沉、機尾抬高，後退時相反；左右移動也會呈現相應滾轉姿態。飛行速度、偏航率與最大高度均設有限制。撞上角錐、樹木、機棚、訓練門或護欄時，馬達會停止、機體翻滾墜落，電腦顯示事故覆蓋畫面，手機則震動並顯示碰撞原因。高速重落地也會判定為墜機，必須重設位置後才能再次解鎖。

## 5. 電腦端視角

點選「相機」可輪流切換：

1. **追蹤**：從無人機後上方平滑追蹤。
2. **環繞**：相機自動繞著無人機旋轉。
3. **俯視**：從正上方觀察位置。

「重設位置」可在無人機飛遠或畫面難以辨識時回到初始位置。

## 6. 手機方向與全螢幕

遙控器始終採橫向配置。手機本身為直向時，CSS 會將整個 UI 順時針旋轉 90 度並交換寬高；手機轉成橫向時則直接填滿畫面。瀏覽器網址列仍可能佔用少量空間，可使用「加入主畫面」或瀏覽器全螢幕功能改善。

## 7. 疑難排解

### QR Code 沒有出現

- 確認電腦能連上網路，並重新整理頁面。
- 確認瀏覽器或內容阻擋器沒有封鎖 `jsdelivr.net` 與 PeerJS。

### 手機一直顯示「正在連線」或「連線失敗」

- 重新整理電腦頁面，再掃描新產生的 QR Code。
- 暫時關閉 VPN、廣告阻擋器或嚴格的追蹤防護。
- 改用一般家用 Wi-Fi 或行動網路測試。公司／校園網路可能封鎖 WebRTC。
- 確認手機開啟的是完整 GitHub Pages HTTPS 網址，不是舊的截圖或已過期頁籤。

### 已連線但無人機不移動

- 先點「解鎖馬達」或「自動起飛」。
- 確認手機頂端仍顯示「已連線」。
- 放開兩個搖桿讓數值歸零，再重新操作。

### 手機排版方向不正確

- 關閉瀏覽器的「電腦版網站」功能。
- 重新整理 controller 頁面。
- 若系統方向鎖定造成瀏覽器縮放異常，將手機實際轉成橫向後再轉回即可。

## 8. 開發與部署

本機請從專案目錄執行：

```bash
python3 -m http.server 8000
```

電腦使用 `http://localhost:8000`；手機測試時，請將 `localhost` 改成電腦在同一區域網路中的 IP。正式部署則在 GitHub 儲存庫的 **Settings → Pages** 選擇從分支根目錄發布。

所有第三方程式庫均由 CDN 載入，沒有 npm 安裝或建置步驟。若要提高正式環境可靠度，可把依賴檔案放進專案、設定自己的 PeerServer，並加入 TURN 伺服器。

## 9. 運作原理：為什麼只有 HTML 也能連線？

HTML 檔案不只可以放文字，也能包含 CSS 與 JavaScript。GitHub Pages 負責用 HTTPS 把這些靜態檔案送到瀏覽器；頁面載入後，真正的 3D 運算、觸控處理和網路連線都由瀏覽器內的 JavaScript 執行。

配對與控制流程如下：

1. 電腦端載入 `index.html`，JavaScript 建立隨機 Peer ID。
2. PeerJS 先透過公用 signaling server 登記這個 ID。signaling server 只協助雙方找到彼此及交換 WebRTC 連線資訊。
3. 電腦把 `controller.html?peer=電腦的PeerID` 完整網址編成 QR Code。
4. 手機掃描後，`controller.html` 從網址讀出 Peer ID，請 PeerJS 連向電腦端。
5. WebRTC DataChannel 建立後，手機把四軸數值與飛行命令直接傳給電腦；電腦則把高度、速度、航向與電量傳回手機。
6. Three.js 每一幀讀取最新控制值，計算簡化的速度、阻尼、位置和旋轉，再重畫 3D 場景。

因此專案不需要自行撰寫後端程式，但並不是完全沒有外部服務：首次配對仍依賴 PeerJS 公用 signaling server，第三方程式庫也由 jsDelivr CDN 載入。多數情況下控制資料會走瀏覽器之間的 WebRTC 點對點連線；遇到嚴格 NAT 或防火牆時，若沒有合適的 TURN 轉送服務，連線仍可能失敗。

手機直向時，CSS 會把整個橫向控制器順時針旋轉 90°。由於 nipplejs 原本以螢幕座標計算手指位移，程式會同步反向旋轉搖桿圖形，並把輸入向量換算回控制器本身的座標軸，避免搖桿與手指相差 90°。
