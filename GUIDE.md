# Web Flight Lab 操作指南

## 1. 開始使用

1. 在電腦開啟模擬器，等待左上角出現 QR Code。
2. 手機掃描 QR Code，待兩端顯示「已連線」。
3. 選擇新手／標準／競速檔及 Mode 1／Mode 2。
4. 點「解鎖馬達」或「自動起飛」。
5. 若要練習水平 8 字，升至約 3 m 後點電腦端「開始評分」。

重新整理電腦頁面會產生新的 Peer ID，手機必須重新掃描。兩台裝置可位於不同網路，但都要能連上網際網路與 PeerJS signaling server。

## 2. 遙控調校與速度限制

遙控調校集中在手機右上「遙控器調校與震動回饋」，避免電腦端面板遮住配對 QR Code。手機調整後會透過資料通道同步到模擬器，並保存在瀏覽器的 `localStorage`。

### 快速檔位

| 檔位 | 最大速度 | 油門上限 | RC Rate | Expo | Deadband | 適用情境 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 新手 | 35% | 65% | 0.65× | 0.45 | 0.08 | 第一次起飛、定點練習 |
| 標準 | 70% | 85% | 1.00× | 0.30 | 0.06 | 一般航線與考照練習 |
| 競速 | 100% | 100% | 1.45× | 0.18 | 0.05 | 熟練後高速與 Acro |

- **最大速度**：同時縮放水平及垂直速度上限，範圍 10%～100%。
- **油門上限**：縮小中心油門到最大推力的可用行程；中心仍保持中性，不會因降低上限而自行下降。
- **RC Rate**：整體軸輸出倍率，範圍 0.1×～2.0×。
- **Expo**：使用 `Output = (1 - Expo) × Input + Expo × Input³`。數值越大，中心越細膩，外圈仍保有完整輸出。
- **Deadband**：忽略搖桿回中漂移，範圍 0～0.20；死區外會重新映射到完整行程。

### 美國手與日本手

| 模式 | 左桿上下 | 左桿左右 | 右桿上下 | 右桿左右 |
| --- | --- | --- | --- | --- |
| 美國手 Mode 2 | Throttle | Yaw | Pitch | Roll |
| 日本手 Mode 1 | Pitch | Yaw | Throttle | Roll |

手機切換模式時會先把四軸復位，避免軸交換造成突發動作。內八解鎖固定依畫面方向判斷：兩桿同時往內下方保持 0.8 秒。

## 3. 手機、Gamepad 與鍵盤

`InputManager` 每個動畫影格都把輸入正規化為 Throttle `0～1`，Yaw／Pitch／Roll `-1～1`。使用中的鍵盤優先，其次是已連接 Gamepad，最後是手機；放開鍵盤即可回到上一種裝置。

### Gamepad／USB 航模遙控器

- 插入裝置後瀏覽器會接收 `gamepadconnected`，拔除時接收 `gamepaddisconnected`。
- Mode 2：左 X = Yaw、左 Y = Throttle、右 X = Roll、右 Y = Pitch。
- Mode 1：左右桿的垂直軸交換。
- A／叉鍵：ARM／DISARM。
- B／圓圈鍵：Reset。

瀏覽器通常要求先按一下手把按鍵才會允許頁面看到裝置。不同廠牌若軸序不符合標準，可能需要在作業系統或遙控器內調整通道映射。

### 鍵盤備援

- `W / S`：油門上升／下降
- `A / D`：向右／向左偏航
- `↑ / ↓`：前進／後退俯仰
- `← / →`：向左／向右橫滾
- `Space`：解鎖／上鎖
- `R`：重設位置
- `M`：切換飛行模式
- `C`：切換相機

## 4. 飛行模式

按 `M` 或電腦端「模式」按鈕循環切換：

1. **自穩 Angle**：搖桿控制目標傾角；回中後以平滑回正力矩恢復水平。
2. **特技 Acro**：搖桿直接控制角速度；回中後保留現有 Pitch／Roll 姿態，適合翻滾練習。
3. **定高 Altitude Hold**：油門接近中心時鎖定當下 Y 高度，以高度誤差與垂直速度回授維持懸停；離開中心可改變高度，回中後重新鎖定。

自動起飛會上升到約 3 m，自動降落則緩降至停機坪。緊急停止立即鎖定動力；這是訓練模擬，不代表真實飛控的緊停策略。

## 5. 風場、亂流、阻力與地面效應

模擬器使用固定基礎風向與風速，物理引擎疊加三個頻率的連續雜訊作為陣風，依「風速向量－機體速度」取得相對氣流，再施加與相對風速平方成比例的阻力。

高度低於 0.5 m 時，升力係數最多增加約 12%，並隨高度連續衰減，模擬旋翼下洗氣流被地面反彈。接近地面時可能感到稍微浮起，屬預期行為。

## 6. 水平 8 字軌跡與評分

場地為 15 m × 15 m，標準路徑由左右兩個半徑 3.2 m 的圓相切構成，航點順序為 P1 → P2 → P3 → P4 → P5 → P6 → P7。

評分卡下方的「評分說明」會開啟全螢幕說明頁，集中顯示航線、高度、合格門檻、航點順序、軌跡顏色及下載說明。可按右上角或頁面底部的「縮小回模擬畫面」、點擊外層背景，或按 `Esc` 返回。

### 開始與完成

1. 將無人機升至目標高度 3 m。
2. 點「開始評分」。
3. 依序飛入航點：P1～P6 使用 1.6 m 檢測半徑，P7 使用較寬鬆的 2.0 m 完成圈。
4. 通過 P7 後自動結算時間、分數及合格結果；60 分以上為 Pass。

### 扣分與 Ghost Trail

- 每 50 ms 記錄一點，保留最近 1000 點。
- 高度誤差 `< 0.5 m` 顯示綠色，`0.5～0.8 m` 黃色，`> 0.8 m` 紅色。
- 與標準 8 字水平距離超過 1.5 m 時才開始緩慢扣分。
- 高度偏離目標 `±0.5 m` 以上時才開始緩慢扣分。
- UI 即時顯示分數、下一航點、時間及路徑偏差。
- 「下載軌跡 CSV」適合用試算表分析；「下載軌跡圖 JPG」會直接生成水平 8 字標準線、P1～P7、依高度誤差著色的實飛軌跡、起終點與飛行摘要，可直接分享或列印。
- 每筆資料包含時間、X／Y／Z、相對高度、路徑誤差、高度誤差、分數、航點及馬達解鎖狀態。

重設位置會清除本次軌跡與評分，若要保留資料請先下載。每次解鎖後必須先離地超過 0.2 m，之後再次碰到地面才視為降落、速度歸零並鎖定馬達；尚未離地時會保持解鎖等待起飛。撞上場地障礙物仍會判定為墜機。

## 7. 多視角

按 `C` 或相機按鈕循環切換：

1. **追隨 Chase**：在機身後上方以 Lerp 平滑跟隨，並反映少量 Roll 傾斜。
2. **FPV**：鏡頭固定於機頭前方，預設向上 20°，會完整跟隨機體姿態。
3. **飛手 Pilot POV**：固定在地面 `[0, 1.7, -8]`，持續 `lookAt` 無人機。
4. **環繞 Orbit**：左鍵平移、右鍵環繞／俯仰、滾輪縮放 3～55 m。

任何滑鼠拖曳或縮放都會先把控制權切到 Orbit；進入其他視角時 Orbit 事件不再改動鏡頭，因此不會發生兩套控制器爭用或切換閃爍。

## 8. 手機震動回饋

手機調校面板可關閉「分級震動」。啟用時：

- 高輸出越過 88%：極短單震，只在跨過門檻時觸發，避免連續轟震。
- 一般命令、上鎖：短單震。
- 解鎖指令送出：短震；收到電腦遙測確認 ARM 狀態由關閉變為開啟後，顯示「馬達解鎖成功」並播放兩段確認震動。
- 通過航點：兩短震。
- 分數跨越每個 5 分區間：警示雙震。
- 完成考核：三段成功節奏。
- 緊急停止或墜機：長短交錯的強警示。

觸覺功能使用 Vibration API。不支援此 API 的瀏覽器（常見於 iPhone／iPad Safari）會直接略過；畫面控制與提示仍正常。部分 Android 瀏覽器要求先由使用者觸碰頁面，之後才能震動。

## 9. 碰撞、安全與遙測

樹木、機棚、訓練門、護欄與角錐都有簡化碰撞體。撞擊後馬達停止、機身翻滾墜落，電腦顯示原因，手機顯示 Toast 並送出事故震動。一般接觸地面則一律判定安全降落並自動鎖定，不再因下降速度判定重落地墜機。

手機頂部顯示高度、合速度、航向與電量；電腦端另回傳飛行模式、目前輸入來源、風速與評分資料供觸覺回饋判斷。

## 10. 手機方向與連線疑難排解

手機直向時，頁面會將完整控制器順時針旋轉 90°，並以 `onMove(-rawY, rawX)` 把 nipplejs 螢幕座標換回控制器座標。搖桿圖形另做反向旋轉，因此圓點仍跟隨手指。

手機 UI 已略微縮小，保留更多搖桿活動空間。Viewport 同時設定最小／最大縮放為 1，並攔截 iOS gesture 與雙擊事件，避免操控時誤觸網頁放大縮小。

展開調校面板時，左右搖桿會暫時停用並將四軸安全歸中，避免拖曳 Rate、速度、Expo 或 Deadband 滑桿時觸控穿透到底層搖桿。關閉調校面板後，搖桿操作會立即恢復。

### QR Code 未出現

- 確認網路與內容阻擋器沒有封鎖 `cdn.jsdelivr.net` 或 PeerJS。
- 重新整理電腦頁面取得新的 Peer ID。

### 手機連不上或中斷

- 重新掃描目前電腦畫面的 QR Code。
- 暫停 VPN、嚴格追蹤防護或廣告阻擋器。
- 公司／校園網路可能封鎖 WebRTC；改用家用 Wi-Fi 或行動網路。

### 已連線但不能飛

- 先 ARM 或自動起飛。
- 放開所有輸入，確認軸值回中。
- 若 Gamepad 持續佔用輸入，拔除後再試手機。
- 墜機後必須 Reset；電量為零時會進入降落流程。

## 11. 開發與部署

本機執行：

```bash
python3 -m http.server 8000
```

本專案是原生 ES6 模組，沒有 npm 建置。GitHub Pages 選擇 `main` 與 `/ (root)` 即可發布。請勿直接用 `file://` 開啟，否則 ES module、WebRTC 或瀏覽器安全規則可能阻擋功能。

資料流程為：電腦建立隨機 Peer ID → QR Code 帶入 `controller.html?peer=...` → PeerJS 協助交換 WebRTC 連線資訊 → DataChannel 傳送原始四軸、調校設定、命令與反向遙測。Rates／Expo／Deadband 統一由電腦端 `InputManager` 套用，避免手機與 Gamepad 出現不同曲線。
